#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../backbone-on-express/bin/backbone" "$@"
  ret=$?
else 
  node  "$basedir/../backbone-on-express/bin/backbone" "$@"
  ret=$?
fi
exit $ret
